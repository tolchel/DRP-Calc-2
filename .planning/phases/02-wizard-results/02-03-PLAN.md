---
phase: 02-wizard-results
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/components/steps/Step2Infrastructure.tsx
autonomous: true
requirements: [INFRA-01, INFRA-02, INFRA-03, INFRA-04, INFRA-05, INFRA-06, INFRA-07, INFRA-08, INFRA-09]

must_haves:
  truths:
    - "Summary info box at top shows total data volume and per-library data volume"
    - "Library cards: first library cannot be deleted; additional libraries have a delete button"
    - "Add Library button (dashed border style) appends a new LibraryConfig"
    - "All infrastructure parameter fields are present with correct defaults and validation"
    - "Inline Russian validation errors shown for drive count and iteration bounds"
  artifacts:
    - path: "src/components/steps/Step2Infrastructure.tsx"
      provides: "Step 2 infrastructure parameter form with library management"
      exports: [Step2Infrastructure]
  key_links:
    - from: "src/components/steps/Step2Infrastructure.tsx"
      to: "src/types/wizard.ts"
      via: "WizardFormData props (libraries, san, engineerCount, etc.)"
      pattern: "WizardFormData|LibraryConfig|SanConfig"
---

<objective>
Build the Step 2 infrastructure form: summary info box, library cards with add/remove, SAN parameters, engineer count, network speeds, uncertainty %, and Monte Carlo iteration count.

Purpose: Step 2 collects all infrastructure parameters that determine HOW fast recovery happens. This data feeds directly into SimulationInput at wizard completion.
Output: src/components/steps/Step2Infrastructure.tsx — fully controlled form component ready to mount in App.tsx.
</objective>

<execution_context>
@/Users/bigpapa/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bigpapa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-wizard-results/02-CONTEXT.md
@src/types/wizard.ts
</context>

<interfaces>
<!-- Types produced by Plan 01 — import these, do not redefine -->

From src/types/wizard.ts (created in Plan 01):
```typescript
export interface LibraryConfig {
  id: string
  name: string
  driveCount: number
  driveThroughputMBs: number
}
export interface SanConfig { maxSpeedGBs: number; streamCount: number; streamSpeedMBs: number }
export interface WizardFormData {
  assets: { db: AssetFormRow; server: AssetFormRow; fs: AssetFormRow; ws: AssetFormRow }
  libraries: LibraryConfig[]
  san: SanConfig
  engineerCount: number
  fastNetworkGbps: number
  lanGbps: number
  uncertaintyPct: number  // stored as % (0-100), NOT 0-1
  trials: number          // 10_000–200_000
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Build Step2Infrastructure component</name>
  <files>src/components/steps/Step2Infrastructure.tsx</files>
  <action>
    Create src/components/steps/Step2Infrastructure.tsx.

    Props interface:
    ```typescript
    interface Props {
      form: Pick<WizardFormData, 'assets' | 'libraries' | 'san' | 'engineerCount' | 'fastNetworkGbps' | 'lanGbps' | 'uncertaintyPct' | 'trials'>
      totalVolumeGB: number   // passed from App.tsx, already computed
      onChange: (patch: Partial<WizardFormData>) => void
      onBack: () => void
      onNext: () => void
    }
    ```

    Full component layout (top to bottom):

    **1. Summary info box** (bg-gray-50 border border-gray-200 rounded-xl p-4 mb-6):
    - "Общий объём данных: {totalVolumeGB.toFixed(2)} ГБ"
    - "Объём на библиотеку: {(totalVolumeGB / form.libraries.length).toFixed(2)} ГБ" — this satisfies INFRA-08
    - text-sm text-gray-600, both lines stacked vertically

    **2. Section heading**: "Ленточные библиотеки" — text-base font-semibold text-gray-800 mb-3

    **3. Library cards** — one card per library (map over form.libraries):
    Card styling: border border-gray-200 rounded-xl p-4 mb-3 relative
    - First row (flex justify-between items-center):
      * Editable name input (text input, value=lib.name, font-medium text-gray-800, borderless or subtle underline style)
      * Delete button (×, lucide-react X icon, text-gray-400 hover:text-red-500) — ONLY shown when index > 0 (first library cannot be deleted per INFRA-01/02)
    - Second row (grid grid-cols-2 gap-4 mt-3):
      * "Количество драйвов": number input, min=1, integer
        - Validation: if value < 1, show error text below "Количество драйвов должно быть ≥ 1" in text-red-500 text-sm
      * "Пропускная способность драйва (МБ/с)": number input, min=1, default=300
    - onChange for any library field: call onChange({ libraries: updated_array })
    - onDelete: filter out library by id, call onChange({ libraries: filtered })

    **4. Add library button** (dashed border style, matches Screen_1 "+" button):
    - Full width, border-2 border-dashed border-gray-300 rounded-xl py-3 text-gray-500 hover:border-blue-400 hover:text-blue-500 transition-colors
    - Label: "+ Добавить библиотеку"
    - onClick: generate new LibraryConfig with id=`lib-${Date.now()}`, name=`Библиотека ${form.libraries.length + 1}`, driveCount=1, driveThroughputMBs=300; call onChange({ libraries: [...form.libraries, newLib] })

    **5. Section heading**: "Параметры СХД" — text-base font-semibold text-gray-800 mt-6 mb-3

    **6. SAN parameters card** (border border-gray-200 rounded-xl p-4):
    Three fields in grid grid-cols-3 gap-4:
    - "Макс. скорость (ГБ/с)": number input, min=0.1, step=0.1
    - "Количество потоков": number input, min=1, integer
    - "Скорость потока (МБ/с)": number input, min=1

    **7. Section heading**: "Дополнительные параметры" — text-base font-semibold text-gray-800 mt-6 mb-3

    **8. Additional parameters card** (border border-gray-200 rounded-xl p-4):
    Grid grid-cols-2 gap-4 for first two rows, then individual rows:
    - Row 1:
      * "Количество IT-инженеров": number input, min=1, integer, default=3
      * (empty or single column)
    - Row 2 (grid-cols-2):
      * "Скоростная сеть (Гбит/с)": number input, min=0.1, step=0.1
      * "ЛВС (Гбит/с)": number input, min=0.1, step=0.1
    - Row 3 (grid-cols-2):
      * "Поправка на неопределённость (%)": number input, min=0, max=100, integer
      * "Итерации Monte Carlo": number input, min=10000, max=200000, step=1000
        - Validation: if value < 10000 or > 200000, show error "Введите значение от 10 000 до 200 000" in text-red-500 text-sm

    All additional parameter changes call onChange({ [field]: newValue }).

    **9. Navigation buttons** (flex justify-between mt-8):
    - Left: "← Назад" button — outline style (border border-gray-300 rounded-lg px-6 py-3 text-gray-700 hover:bg-gray-50), calls onBack()
    - Right: "Далее: Результаты →" button — filled blue (bg-blue-600 hover:bg-blue-700 text-white rounded-lg px-6 py-3 font-medium), calls onNext()
      - onNext is disabled if any validation error exists (driveCount < 1 on any library, or iterations out of range)

    Fully controlled component — no internal useState. Import WizardFormData, LibraryConfig from '../../types/wizard'. Import X icon from 'lucide-react'.

    Validation: Compute a `hasErrors` boolean from: any library with driveCount < 1, or trials < 10000, or trials > 200000. Set "Далее: Результаты →" button disabled + opacity-50 cursor-not-allowed when hasErrors is true.
  </action>
  <verify>
    <automated>cd /Users/bigpapa/Documents/Vibe_Code/DRP-Calc-2 && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Step2Infrastructure.tsx compiles cleanly; exports Step2Infrastructure; all INFRA fields present; library add/delete logic correct (first undeletable); validation errors in Russian; npx tsc --noEmit exits 0</done>
</task>

</tasks>

<verification>
- npx tsc --noEmit exits 0
- Step2Infrastructure exports named Step2Infrastructure component
- Summary info box shows per-library volume (INFRA-08)
- First library has no delete button (INFRA-01, INFRA-02)
- Validation errors in Russian (INFRA-09)
- All parameter fields present: libraries, SAN (INFRA-03), engineers (INFRA-04), networks (INFRA-05), uncertainty (INFRA-06), iterations (INFRA-07)
</verification>

<success_criteria>
- All nine INFRA requirements (INFRA-01 through INFRA-09) are addressed by the form fields and behaviors
- Library management (add/remove/edit) works correctly
- Russian inline validation errors for drive count and iteration bounds
- TypeScript compilation passes
</success_criteria>

<output>
After completion, create .planning/phases/02-wizard-results/02-03-SUMMARY.md
</output>
