---
phase: 02-wizard-results
plan: 05
type: execute
wave: 4
depends_on: [02-04]
files_modified:
  - src/components/ResultsPage.tsx
  - src/App.tsx
autonomous: false
requirements: [VIZ-01, VIZ-02, VIZ-03, VIZ-04, VIZ-05]

must_haves:
  truths:
    - "Two side-by-side scenario panels visible: Кибербакап (left) and Конкурент (right)"
    - "Each panel shows 4 metric cards: Лучший, Худший, Вероятность лучшего, Среднее значение"
    - "Each panel has a recharts AreaChart KDE curve with X-axis in hours and vertical dashed lines for best/worst"
    - "Hovering the chart shows time value and density at cursor position"
    - "Shared asset breakdown table appears below both panels"
    - "Back button returns to Step 2 with all form values intact"
  artifacts:
    - path: "src/components/ResultsPage.tsx"
      provides: "Full results view: two scenario panels + asset table"
      exports: [ResultsPage]
  key_links:
    - from: "src/components/ResultsPage.tsx"
      to: "recharts"
      via: "AreaChart with KDEPoint[] data"
      pattern: "AreaChart|ComposedChart|recharts"
    - from: "src/App.tsx"
      to: "src/components/ResultsPage.tsx"
      via: "replaces ResultsPlaceholder when currentStep===3"
      pattern: "ResultsPage"
---

<objective>
Build the Results page with two side-by-side scenario panels (metric cards + KDE density charts) and a shared asset breakdown table. Wire it into App.tsx, replacing the ResultsPlaceholder.

Purpose: This is the payoff — the visual comparison that convinces the customer. Кибербакап's KDE curve peaking earlier (lower hours) than Конкурент's is the core value proposition of the tool.
Output: src/components/ResultsPage.tsx + App.tsx updated to use it. Human verification confirms chart renders correctly.
</objective>

<execution_context>
@/Users/bigpapa/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bigpapa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-wizard-results/02-CONTEXT.md
@src/types/simulation.ts
@src/types/wizard.ts
@src/App.tsx
@.planning/phases/01-foundation-engine/01-05-SUMMARY.md
</context>

<interfaces>
<!-- Locked simulation types — all time values in HOURS -->

From src/types/simulation.ts:
```typescript
export interface KDEPoint { x: number; density: number }  // x in HOURS
export interface ScenarioResult {
  kde: KDEPoint[]   // 200-300 points, x in HOURS
  p10: number       // 10th percentile, hours
  p50: number       // median, hours
  p90: number       // 90th percentile, hours
  min: number       // best case, hours
  max: number       // worst case, hours
}
export interface SimulationResult { kyberbackup: ScenarioResult; competitor: ScenarioResult }
```

From src/types/wizard.ts:
```typescript
export interface AssetFormRow { count: number; avgSizeGB: number }
export interface WizardFormData {
  assets: { db: AssetFormRow; server: AssetFormRow; fs: AssetFormRow; ws: AssetFormRow }
  // ...
}
```

recharts AreaChart for KDE (key usage pattern):
```typescript
import { AreaChart, Area, XAxis, YAxis, CartesianGrid, Tooltip, ReferenceLine, ResponsiveContainer } from 'recharts'
// kde: KDEPoint[] — feed directly as data prop
// x key: 'x' (hours), density key: 'density'
// ReferenceLine for best/worst vertical dashed lines
// Tooltip: customizable, shows x (hours) and density
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Build ResultsPage component</name>
  <files>src/components/ResultsPage.tsx</files>
  <action>
    Create src/components/ResultsPage.tsx.

    Props:
    ```typescript
    interface Props {
      result: SimulationResult
      formData: WizardFormData   // needed for asset breakdown table
      onBack: () => void
    }
    ```

    --- METRIC CARDS ---

    Each scenario panel has 4 metric cards. Helper to format hours: format hours as "Xч YYм" (e.g., "2ч 15м") or just "Xч" if minutes=0. Use: `Math.floor(hours)` for hours, `Math.round((hours % 1) * 60)` for minutes.

    Four metric cards per panel (following Screen_Report color scheme from CONTEXT.md):
    1. "Лучший сценарий" — value = scenario.min, color = green (text-green-600, bg-green-50 border-green-100)
    2. "Худший сценарий" — value = scenario.max, color = red (text-red-600, bg-red-50 border-red-100)
    3. "Вероятность лучшего" — value = scenario.p10, label "10-й перцентиль", color = blue (text-blue-600, bg-blue-50 border-blue-100)
    4. "Среднее значение" — value = scenario.p50, label "Медиана", color = purple (text-purple-600, bg-purple-50 border-purple-100)

    Card layout: border rounded-xl p-4, label text-xs text-gray-500 mb-1, value text-2xl font-bold colored-number, unit text-sm text-gray-400 below value.

    --- KDE CHART ---

    Use recharts `ComposedChart` (more flexible than AreaChart for overlaid reference lines). Import from 'recharts': ComposedChart, Area, XAxis, YAxis, CartesianGrid, Tooltip, ReferenceLine, ResponsiveContainer, Legend.

    Chart config per scenario panel:
    ```typescript
    <ResponsiveContainer width="100%" height={220}>
      <ComposedChart data={scenario.kde} margin={{ top: 10, right: 20, left: 0, bottom: 20 }}>
        <CartesianGrid strokeDasharray="3 3" stroke="#f0f0f0" />
        <XAxis
          dataKey="x"
          type="number"
          domain={['auto', 'auto']}
          tickFormatter={(v: number) => `${v.toFixed(1)}ч`}
          label={{ value: 'Время восстановления (часы)', position: 'insideBottom', offset: -10, fontSize: 11, fill: '#6b7280' }}
        />
        <YAxis
          dataKey="density"
          tickFormatter={(v: number) => v.toFixed(3)}
          label={{ value: 'Плотность вероятности', angle: -90, position: 'insideLeft', offset: 10, fontSize: 11, fill: '#6b7280' }}
          width={60}
        />
        <Tooltip
          formatter={(value: number, name: string) => [value.toFixed(4), name === 'density' ? 'Плотность' : name]}
          labelFormatter={(label: number) => `Время: ${label.toFixed(2)}ч`}
        />
        <Area
          type="monotone"
          dataKey="density"
          stroke={panelColor}
          fill={panelFill}
          fillOpacity={0.3}
          strokeWidth={2}
          dot={false}
          name="Распределение вероятности"
        />
        <ReferenceLine x={scenario.min} stroke="#22c55e" strokeDasharray="4 4" strokeWidth={2}
          label={{ value: 'Лучший', position: 'insideTopRight', fill: '#22c55e', fontSize: 11 }} />
        <ReferenceLine x={scenario.max} stroke="#ef4444" strokeDasharray="4 4" strokeWidth={2}
          label={{ value: 'Худший', position: 'insideTopLeft', fill: '#ef4444', fontSize: 11 }} />
      </ComposedChart>
    </ResponsiveContainer>
    ```

    Кибербакап panel color: stroke="#2563eb" fill="#93c5fd" (blue)
    Конкурент panel color: stroke="#7c3aed" fill="#c4b5fd" (purple)

    --- PANEL LAYOUT ---

    Each ScenarioPanel (create as inner component or just inline): titled section with scenario name + 4 metric cards grid (grid-cols-2 gap-3 mb-4) + chart.

    Two panels side by side: grid grid-cols-2 gap-6 (responsive: on small screens stack — but desktop-first tool, so grid-cols-2 is fine)

    Panel 1: result.kyberbackup — title "Кибербакап" text-blue-700 font-semibold text-lg
    Panel 2: result.competitor — title "Конкурент" text-purple-700 font-semibold text-lg

    --- ASSET BREAKDOWN TABLE ---

    Below the two panels: "Разбивка по типам активов" heading (text-base font-semibold text-gray-800 mt-8 mb-3)

    Table (w-full border-collapse text-sm):
    ```
    | Тип системы          | Количество | Объём на актив (ГБ) | Общий объём (ГБ) |
    |----------------------|------------|---------------------|------------------|
    | Базы данных          | N          | X.X                 | Y.Y              |
    | Серверы              | N          | X.X                 | Y.Y              |
    | Файловые хранилища   | N          | X.X                 | Y.Y              |
    | Рабочие станции      | N          | X.X                 | Y.Y              |
    ```

    Show ALL four rows regardless of zero values. Table header: bg-gray-50, border-b border-gray-200. Rows: even rows bg-white, odd rows bg-gray-50/30.
    Asset type labels: db→"Базы данных", server→"Серверы", fs→"Файловые хранилища", ws→"Рабочие станции"
    Render in ASSET_PRIORITY order: ['db', 'server', 'fs', 'ws'] — import ASSET_PRIORITY from '../engine/constants'.

    --- BACK BUTTON ---

    Below table: "← Вернуться к настройкам" button — border border-gray-300 rounded-lg px-6 py-2 text-gray-700 hover:bg-gray-50, calls onBack().

    --- PAGE WRAPPER ---

    Outer div: no extra padding needed (App.tsx main provides max-w-2xl px-4 py-8).

    Export: export default function ResultsPage({ result, formData, onBack }: Props)
    Imports: SimulationResult, KDEPoint from '../types/simulation'; WizardFormData from '../types/wizard'; ASSET_PRIORITY from '../engine/constants'; recharts components from 'recharts'.
  </action>
  <verify>
    <automated>cd /Users/bigpapa/Documents/Vibe_Code/DRP-Calc-2 && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>ResultsPage.tsx compiles; exports ResultsPage; has two panels with metric cards and recharts charts; asset breakdown table; back button; npx tsc --noEmit exits 0</done>
</task>

<task type="auto">
  <name>Task 2: Wire ResultsPage into App.tsx</name>
  <files>src/App.tsx</files>
  <action>
    Update src/App.tsx to:
    1. Import ResultsPage from './components/ResultsPage'
    2. Remove the inline ResultsPlaceholder function
    3. Replace the `{currentStep === 3 && <ResultsPlaceholder ... />}` block with:

    ```jsx
    {currentStep === 3 && (
      isRunning ? (
        // Progress display while simulation runs
        <div className="text-center py-16">
          <p className="text-gray-600 mb-4 text-lg">Выполняется симуляция...</p>
          <div className="w-full bg-gray-200 rounded-full h-3 max-w-sm mx-auto">
            <div
              className="bg-blue-600 h-3 rounded-full transition-all duration-300"
              style={{ width: `${progress ?? 0}%` }}
            />
          </div>
          <p className="text-sm text-gray-400 mt-3">{progress ?? 0}%</p>
        </div>
      ) : result ? (
        <ResultsPage
          result={result}
          formData={formData}
          onBack={() => setCurrentStep(2)}
        />
      ) : (
        // Empty state (shouldn't normally appear — simulation runs on step advance)
        <div className="text-center py-16 text-gray-400">
          <p>Симуляция не запущена</p>
          <button
            onClick={() => setCurrentStep(2)}
            className="mt-4 border border-gray-300 rounded-lg px-6 py-2 text-gray-700 hover:bg-gray-50"
          >
            ← Назад
          </button>
        </div>
      )
    )}
    ```

    Also change Step 3 in ProgressBar: ProgressBar currentStep prop should be `currentStep` (already correct — no change needed).

    After edit, verify npx tsc --noEmit still exits 0.
  </action>
  <verify>
    <automated>cd /Users/bigpapa/Documents/Vibe_Code/DRP-Calc-2 && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>App.tsx imports and renders ResultsPage when result is available; shows progress bar while simulation runs; ResultsPlaceholder removed; tsc exits 0</done>
</task>

<task type="auto">
  <name>Task 3: Build dist and run smoke check</name>
  <files></files>
  <action>
    Run the production build to confirm single-file output still holds after all Phase 2 additions:

    cd /Users/bigpapa/Documents/Vibe_Code/DRP-Calc-2 && npm run build

    Then verify:
    1. dist/index.html exists
    2. No other JS/CSS files in dist/ (single-file constraint UX-03)
    3. File size is reported — note it in SUMMARY (recharts adds significant weight, expect 400-700kB range)

    Check commands:
    ```bash
    ls -la dist/
    wc -c dist/index.html
    ```

    If build fails with TypeScript errors, fix them before proceeding. If build succeeds but dist/ contains chunk files (dist/assets/*.js), that violates UX-03 — do NOT accept this. In that case check vite.config.ts still has vite-plugin-singlefile applied.
  </action>
  <verify>
    <automated>cd /Users/bigpapa/Documents/Vibe_Code/DRP-Calc-2 && npm run build 2>&1 | tail -10 && ls dist/</automated>
  </verify>
  <done>npm run build succeeds; dist/ contains only index.html (single-file constraint preserved); file size noted</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete 3-step wizard with:
    - Step 1: 4 asset cards with subtotals + blue grand-total footer + disabled Next when total=0
    - Step 2: Summary info box + library cards (add/remove) + SAN/engineer/network/uncertainty/iterations fields + Russian validation
    - Step 3: Two side-by-side scenario panels (Кибербакап + Конкурент) each with 4 colored metric cards + KDE AreaChart with vertical dashed lines + shared asset breakdown table
    - Progress bar at top tracking current step
    - Back/Next navigation preserving all form values
  </what-built>
  <how-to-verify>
    1. Open dist/index.html by double-clicking (file:// origin — no server needed)
    2. Step 1: Enter values — e.g., Базы данных: 5 assets, 200 GB each. Verify subtotal shows "1000.0 ГБ" and footer shows "1000.00 ГБ (0.98 ТБ)". Verify "Далее" is enabled.
    3. Click "Далее" — Step 2 should appear. Verify progress bar shows Step 2 as active, Step 1 as completed.
    4. Step 2: Verify summary box shows "Общий объём данных: 1000.00 ГБ" and "Объём на библиотеку: 1000.00 ГБ".
    5. Click "+ Добавить библиотеку" — second library card appears with delete button. First library has no delete button.
    6. Click "← Назад" — returns to Step 1 with all values preserved (Базы данных still shows 5 / 200).
    7. Click "Далее" again, then "Далее: Результаты →" — progress spinner appears, then results page loads.
    8. Results page: verify two panels labeled "Кибербакап" and "Конкурент" side by side. Each should have 4 metric cards and a chart.
    9. Hover over either KDE curve — tooltip should appear showing time in hours and density value.
    10. Verify green dashed "Лучший" and red dashed "Худший" vertical lines appear on both charts.
    11. Verify asset breakdown table below panels shows row for Базы данных with count=5, avgSize=200, total=1000.
    12. Кибербакап p50 should be LESS than Конкурент p50 (the tool's core value proposition).
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
- npm run build produces single dist/index.html with no chunk files
- npx tsc --noEmit exits 0
- ResultsPage renders two panels with recharts charts
- Asset breakdown table in correct priority order (db, server, fs, ws)
- Human-verified: wizard flow, chart tooltips, metric cards, back navigation
</verification>

<success_criteria>
- KDE density chart renders with X-axis in hours, Y-axis as density (VIZ-01, VIZ-02)
- Metric cards show best/worst/p50/p10 per scenario (VIZ-03)
- Asset breakdown table shows all four asset types with counts and volumes (VIZ-04)
- Chart hover tooltip shows time and density (VIZ-05)
- Single dist/index.html preserved (UX-03 still passing)
- Кибербакап p50 visibly lower than Конкурент p50 — tool's core value prop confirmed
</success_criteria>

<output>
After completion, create .planning/phases/02-wizard-results/02-05-SUMMARY.md
</output>
