---
phase: 02-wizard-results
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/components/steps/Step1Assets.tsx
autonomous: true
requirements: [ASSET-01, ASSET-02, ASSET-03, ASSET-04]

must_haves:
  truths:
    - "User sees four asset cards: Базы данных, Серверы, Файловые хранилища, Рабочие станции"
    - "Each card shows count field and avgSizeGB field side by side with per-card subtotal"
    - "Blue footer shows grand total volume in GB and TB"
    - "Next button is disabled when total volume is zero"
  artifacts:
    - path: "src/components/steps/Step1Assets.tsx"
      provides: "Step 1 asset entry form with 4 cards and blue footer"
      exports: [Step1Assets]
  key_links:
    - from: "src/components/steps/Step1Assets.tsx"
      to: "src/types/wizard.ts"
      via: "WizardFormData.assets prop"
      pattern: "WizardFormData|AssetFormRow"
---

<objective>
Build the Step 1 asset form: four cards (one per asset type), per-card subtotals, blue grand-total footer, and disabled-Next guard.

Purpose: Step 1 is where the user enters how much data needs to be recovered. All four asset types feed into SimulationInput.assets after the wizard completes. This is the first form the user sees.
Output: src/components/steps/Step1Assets.tsx — a self-contained form step component ready to be mounted by App.tsx in Plan 04.
</objective>

<execution_context>
@/Users/bigpapa/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bigpapa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-wizard-results/02-CONTEXT.md
@src/types/wizard.ts
</context>

<interfaces>
<!-- Types produced by Plan 01 — import these, do not redefine -->

From src/types/wizard.ts (created in Plan 01):
```typescript
export interface AssetFormRow { count: number; avgSizeGB: number }
export interface WizardFormData {
  assets: { db: AssetFormRow; server: AssetFormRow; fs: AssetFormRow; ws: AssetFormRow }
  // ... rest of WizardFormData
}
export const DEFAULT_WIZARD_DATA: WizardFormData
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Build Step1Assets component</name>
  <files>src/components/steps/Step1Assets.tsx</files>
  <action>
    Create src/components/steps/Step1Assets.tsx.

    Props interface:
    ```typescript
    interface Props {
      assets: WizardFormData['assets']
      onChange: (assets: WizardFormData['assets']) => void
      onNext: () => void
    }
    ```

    Layout (matches Screen_1 design from CONTEXT.md):
    - Page wrapper: white background, max-w-2xl centered, px-6 py-8
    - Heading: "Шаг 1: Активы для восстановления" — h2, text-xl font-semibold mb-6
    - Four asset cards in a vertical stack (gap-4), one per asset type in this order: db, server, fs, ws
    - Fixed blue footer at the bottom of the step (NOT position:fixed — just a styled div after the cards)

    Each card (asset type card):
    - White card with border border-gray-200 rounded-xl p-5 shadow-sm
    - Card header: asset type label (see labels below) — text-base font-medium text-gray-800 mb-4
    - Two inputs side by side (grid grid-cols-2 gap-4):
      * "Количество активов": number input, min=0, integer, placeholder="0"
      * "Средний объём на актив (ГБ)": number input, min=0, step=0.1, placeholder="0"
    - Each input has a label above it: text-sm text-gray-500 mb-1
    - Per-card subtotal below the inputs: text-sm text-gray-500 mt-3
      Format: "Общий объём: {subtotal.toFixed(1)} ГБ"
      subtotal = count × avgSizeGB

    Asset type labels:
    - db → "Базы данных"
    - server → "Серверы"
    - fs → "Файловые хранилища"
    - ws → "Рабочие станции"

    Blue footer (after the cards):
    - bg-blue-600 text-white rounded-xl p-4 mt-6
    - Left text: "Общий объём данных для восстановления:"
    - Right text (bold): "{totalGB.toFixed(2)} ГБ ({totalTB.toFixed(2)} ТБ)"
      where totalGB = sum of all four subtotals, totalTB = totalGB / 1024
    - Display as flex justify-between items-center

    Next button:
    - Placed after the blue footer
    - Full width, mt-4
    - Label: "Далее: Инфраструктура →"
    - Styling: bg-blue-600 hover:bg-blue-700 text-white font-medium py-3 rounded-lg transition-colors
    - DISABLED when totalGB === 0: opacity-50 cursor-not-allowed, onClick does nothing
    - When enabled: calls onNext()

    onChange handler: when any input changes, call onChange with the updated assets object (spread existing, update only the changed row key).

    Import: WizardFormData from '../../types/wizard'. Use lucide-react if icons are desired but none are required.
    No useState needed inside this component — it is fully controlled (all state lives in App.tsx per locked decision).
  </action>
  <verify>
    <automated>cd /Users/bigpapa/Documents/Vibe_Code/DRP-Calc-2 && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Step1Assets.tsx compiles cleanly; exports Step1Assets; has four asset cards with subtotals; blue grand-total footer; Next button disabled when totalGB===0; npx tsc --noEmit exits 0</done>
</task>

</tasks>

<verification>
- npx tsc --noEmit exits 0
- Step1Assets.tsx exports Step1Assets as default or named export
- Component is fully controlled (no internal useState for asset values)
- Four cards in correct order: db, server, fs, ws
- Next button disabled logic: totalGB === 0
</verification>

<success_criteria>
- Step1Assets renders 4 asset cards with per-card subtotals (ASSET-01, ASSET-02)
- Grand total footer shows sum in GB and TB (ASSET-03)
- Next button disabled when total volume is zero (ASSET-04)
- TypeScript compilation passes
</success_criteria>

<output>
After completion, create .planning/phases/02-wizard-results/02-02-SUMMARY.md
</output>
