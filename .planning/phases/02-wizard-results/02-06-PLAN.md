---
phase: 02-wizard-results
plan: "06"
type: execute
wave: 6
depends_on: ["02-05"]
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [VIZ-04]
gap_closure: true

must_haves:
  truths:
    - "REQUIREMENTS.md VIZ-04 describes the asset breakdown table as showing input data (count, average size GB, total GB per asset type)"
    - "REQUIREMENTS.md VIZ-04 no longer references timing data (базовое время, среднее из триалов)"
    - "The requirement text matches the implemented table columns in ResultsPage.tsx"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated VIZ-04 requirement text"
      contains: "Таблица с разбивкой по типам активов"
  key_links:
    - from: ".planning/REQUIREMENTS.md VIZ-04"
      to: "src/components/ResultsPage.tsx asset table"
      via: "requirement describes what is actually rendered"
      pattern: "Количество.*Объём"
---

<objective>
Close the VIZ-04 verification gap by updating REQUIREMENTS.md to reflect the user's locked decision from CONTEXT.md.

Purpose: The verification found 1/21 truths failing because REQUIREMENTS.md line 45 says "базовое время, среднее из триалов" (timing data per asset type), but the user explicitly decided in CONTEXT.md that the asset breakdown table shows "Тип системы / Количество / Объём на актив (ГБ) / Общий объём (ГБ)" — input data. Plan 05 correctly implemented the user's decision. The requirement text is the only thing out of sync.

Output: REQUIREMENTS.md with VIZ-04 updated to match the implemented behavior and the user's locked decision. No code changes needed.
</objective>

<execution_context>
@/Users/bigpapa/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bigpapa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-wizard-results/02-CONTEXT.md

<!-- Key decision from CONTEXT.md (locked, authoritative user intent):
Line 54: "Shared asset breakdown table below both panels (VIZ-04):
columns Тип системы / Количество / Объём на актив (ГБ) / Общий объём (ГБ)"

Current REQUIREMENTS.md line 45 (to be updated):
  - [x] **VIZ-04**: Таблица с разбивкой по типам активов: базовое время, среднее из триалов

Target text after update:
  - [x] **VIZ-04**: Таблица с разбивкой по типам активов: Тип системы / Количество / Объём на актив (ГБ) / Общий объём (ГБ) — входные данные для каждого типа актива
-->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update VIZ-04 in REQUIREMENTS.md to match user decision</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Edit line 45 of .planning/REQUIREMENTS.md. The current text is:

  - [x] **VIZ-04**: Таблица с разбивкой по типам активов: базовое время, среднее из триалов

Replace with:

  - [x] **VIZ-04**: Таблица с разбивкой по типам активов: столбцы — Тип системы / Количество / Объём на актив (ГБ) / Общий объём (ГБ)

Rationale to document via code comment is not applicable here (it is a plain text file), but the change aligns with the locked user decision in 02-CONTEXT.md line 54, which specified these exact columns. The original requirement text ("базовое время, среднее из триалов") described timing output per asset type, which the engine's ScenarioResult does not produce and which the user overrode when deciding on the input-data column layout.

No other lines in REQUIREMENTS.md need to change. Do not modify the Traceability table (VIZ-04 is already marked Complete there).
  </action>
  <verify>
    <automated>grep "VIZ-04" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>The grep output shows VIZ-04 with "Тип системы" and "Объём на актив" in the description, and no mention of "базовое время" or "триалов".</done>
</task>

</tasks>

<verification>
After the task completes:

1. `grep "VIZ-04" .planning/REQUIREMENTS.md` shows the updated column description with no timing references.
2. `grep "базовое время" .planning/REQUIREMENTS.md` returns no matches.
3. The Traceability table still shows VIZ-04 as Complete in Phase 2.
</verification>

<success_criteria>
- REQUIREMENTS.md VIZ-04 describes the asset breakdown table as showing input data columns (Тип системы / Количество / Объём на актив (ГБ) / Общий объём (ГБ))
- No reference to timing data ("базовое время", "среднее из триалов") remains in VIZ-04
- The requirement now matches what ResultsPage.tsx renders and what the user decided in CONTEXT.md
- The VIZ-04 verification truth can now pass on re-verification
</success_criteria>

<output>
After completion, create `.planning/phases/02-wizard-results/02-06-SUMMARY.md` with:
- What changed: VIZ-04 requirement text updated
- Why: User's locked CONTEXT.md decision specifies input-data columns, not timing columns
- Gap status: VIZ-04 gap closed — REQUIREMENTS.md now matches implementation
</output>
